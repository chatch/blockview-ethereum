peer.discovery = {
    enabled = false
}

database {
    dir = /tmp/blockview
    reset = true
}

dump {
    full = false
    dir = /tmp/dmp
    clean.on.restart = true
}

redis.enabled=false

record.blocks=false

sync {
    enabled = false
}
